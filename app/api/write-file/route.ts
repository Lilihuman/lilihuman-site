import{NextRequest,NextResponse}from"next/server";import fs from"fs";import path from"path";export async function POST(r:NextRequest){try{const b=await r.json();if(b.list){const files=fs.readdirSync(path.resolve(b.dir)).filter((f:string)=>f.toLowerCase().includes((b.filter||'').toLowerCase()));return NextResponse.json({ok:true,files})}if(b.copy){const src=b.absolute?path.resolve(b.from):path.resolve(b.from);const dest=path.join(process.cwd(),b.to);fs.mkdirSync(path.dirname(dest),{recursive:true});fs.copyFileSync(src,dest);return NextResponse.json({ok:true,action:"copy"})}if(b.read){const c=fs.readFileSync(path.join(process.cwd(),b.path),"utf8");return NextResponse.json({ok:true,content:c})}const p=path.join(process.cwd(),b.path);fs.mkdirSync(path.dirname(p),{recursive:true});fs.writeFileSync(p,b.content,"utf8");return NextResponse.json({ok:true})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}